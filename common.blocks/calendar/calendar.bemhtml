block('calendar')(
    def()(function() {
        applyNext({ _ctx: this.ctx });
    }),
    js()(function() {
        var ctx = this.ctx;
        return {
            year: ctx.year,
            month: ctx.month,
            date: ctx.date
        }
    }),
    content()([
        { elem: 'month-select' },
        { elem: 'grid' }
    ]),
    elem('row').tag()('tr'),
    elem('day-name').tag()('th'),
    elem('day').tag()('td')
)
